import generatePwdForm from './modules/generatePwdForm';
import './assets/css/style.css';

generatePwdForm();

/* document.addEventListener('click', (e) => {
  const el = e.target;

  if (!el.classList.contains('generate')) return;
}); */